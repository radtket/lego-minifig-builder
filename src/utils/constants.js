export const defaultSliderValues = {
  expression: 0,
  upperHue: 200,
  upperSaturation: 0,
  upperLightness: 90,
  lowerHue: 200,
  lowerSaturation: 0,
  lowerLightness: 90,
};

export const inputConfig = {
  expression: {
    max: 500,
    min: 0,
    step: 100,
  },
  hue: {
    max: 360,
    min: 0,
  },
  saturation: {
    max: 100,
    min: 0,
  },
  lightness: {
    max: 90,
    min: 0,
  },
};
